VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
# Never add SUPABASE_SERVICE_ROLE_KEY here â€” it bypasses RLS and must stay server-only (Edge Functions auto-inject it)

# Manual MoMo (Stage 7) — placeholder, configure with real Cartiva MoMo details before production
VITE_MOMO_NETWORK=
VITE_MOMO_ACCOUNT_NAME=
VITE_MOMO_NUMBER=

